# Inspection Rover

For the TAROS paper, use `config_rover5_full....yaml`

* ROVER_SYSTEM is wrong, it can't do rad_level.green first (which is the first event in the log)
* ROVER_SYSTEM2 seems ok, barring the alphabet mismatch
